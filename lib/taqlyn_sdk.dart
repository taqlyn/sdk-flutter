/// Taqlyn Flutter plugin — thin wrapper over native Android/iOS SdkCore.
///
/// Pub name: `taqlyn_sdk` (aligned with product). Import this package only
/// from app feature code — never Play Install Referrer / UIPasteboard.
library;

export 'src/taqlyn_sdk.dart';
export 'src/platform_links.dart';
